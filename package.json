{
  "name": "suisei",
  "private": true,
  "version": "0.0.0",
  "description": "Suisei - Sui's agent toolkit. The @suisei-mcp/mcp MCP server and the @suisei-mcp/agent-signer non-custodial signer, plus design docs for the showcase apps that build on them.",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm --workspaces run build",
    "build:mcp": "npm --workspace @suisei-mcp/mcp run build",
    "build:signer": "npm --workspace @suisei-mcp/agent-signer run build"
  }
}
