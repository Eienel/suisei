# --- Server-side (Vercel Function, never exposed to browser) ---
# Anthropic API key for the AI Builder Agent (/api/agent).
ANTHROPIC_API_KEY=

# --- Client-side (safe to expose; Vite inlines at build) ---
# Sui network: 'testnet' | 'devnet' | 'mainnet'
VITE_SUI_NETWORK=testnet

# Enoki — Mysten's zkLogin service.
# Sign up at https://portal.enoki.mystenlabs.com (free tier)
VITE_ENOKI_API_KEY=

# Google OAuth Web client ID — used by Enoki for "Sign in with Google".
# Create at https://console.cloud.google.com → APIs & Services → Credentials.
# Authorized JS origin must include your Vercel domain.
VITE_ENOKI_GOOGLE_CLIENT_ID=

# Badge Move package ID. Module: suisei::badge (mint entry fun).
# Publish with scripts/publish-badge.sh, then paste the package id here.
# When blank, all quest badge mints are mocked locally (dev mode).
VITE_BADGE_PACKAGE_ID=

# Sponsor service base URL for Quest 2 (Enoki-shaped two-step flow:
# POST /sponsor, POST /execute). When blank, sponsored mints are mocked.
VITE_SPONSOR_URL=

# DeepBook indexer (Quest 8 reads the live orderbook from here).
# Defaults to the public testnet/mainnet indexer per VITE_SUI_NETWORK.
VITE_DEEPBOOK_INDEXER_URL=
# Pool name in indexer convention, e.g. SUI_DBUSDC (testnet) / SUI_USDC (mainnet).
VITE_DEEPBOOK_POOL=

# Walrus testnet endpoints (optional — sensible defaults used if blank).
# https://docs.walrus.site/usage/web-api.html
VITE_WALRUS_PUBLISHER_URL=
VITE_WALRUS_AGGREGATOR_URL=
